# these are required for running manim

# install FFmpeg
ffmpeg

# install Cairo
libcairo2
libcairo2-dev

# install SoX
sox

# install LaTeX
texlive
texlive-latex-extra
texlive-fonts-extra
texlive-latex-recommended
texlive-science
tipa
